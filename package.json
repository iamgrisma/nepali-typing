{
  "name": "nepali-typing",
  "type": "module",
  "version": "1.0.0",
  "description": "Nepali Typing Test Platform - English, Unicode Traditional, Romanized & Preeti ASCII",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 3000",
    "start": "astro preview --host 0.0.0.0 --port 3000",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist --project-name=nepali-typing"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.2",
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "wrangler": "^4.136.2"
  }
}
